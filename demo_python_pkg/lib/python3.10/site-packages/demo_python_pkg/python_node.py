import rclpy
from rclpy.node import Node

def main():
    rclpy.init() #初始化工作
    node = Node('python_node')
    node.get_logger().info("hello node?")
    node.get_logger().warn("hello node")
    rclpy.spin(node)
    rclpy.shutdown()
